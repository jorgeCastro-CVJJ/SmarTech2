const db = require("../util/database");
const bcrypt = require("bcryptjs");

//crear el objeto
module.exports = class Tareas {
    constructor(nombreT, idTarea, horasTrabajo, idProyecto) {
        
    }
}